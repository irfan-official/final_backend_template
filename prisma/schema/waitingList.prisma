model WaitingList {
  id        String        @id @default(auto()) @map("_id") @db.ObjectId
  email     String
  status    WaitingStatus @default(ACTIVE)
  createdAt DateTime      @default(now())
  updatedAt DateTime      @updatedAt
}
